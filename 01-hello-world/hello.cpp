//including libraries
#include <iostream>

//the main() function with {} <- marking the beggining and the end of it
int main() {
    /*
    std = standard library | cout = console outpout 
    the cout command is part of the std lib and it displays text to the terminal for the user to see
    \n is the breakline short
    */
    std::cout << "Hello World\n";
}